{% extends 'base.html.twig' %}

{% block title %}ProductType{% endblock %}

{% block body %}
    <h1>ProductType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_type.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product_type.price }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ product_type.createAt ? product_type.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ product_type.updateAt ? product_type.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ product_type.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_type_index') }}">back to list</a>

    <a href="{{ path('app_product_type_edit', {'id': product_type.id}) }}">edit</a>

    {{ include('product_type/_delete_form.html.twig') }}
{% endblock %}
